============
Installation
============

At the command line::

    $ easy_install django-middlewall

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-middlewall
    $ pip install django-middlewall
